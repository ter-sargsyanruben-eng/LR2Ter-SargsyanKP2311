# Project Overview

A Python Flask web application.

## Recent Changes
- 2026-02-24: Initialized `main.py` with a basic Flask app (was previously empty). Installed Flask dependency.

## Project Architecture
- **main.py** — Flask application entry point, runs on port 5000.
- **pyproject.toml** — Project metadata and dependencies (Flask).

## User Preferences
(none recorded yet)
